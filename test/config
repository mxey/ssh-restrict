[commands]
simple = echo TEST_SIMPLE
not_matching = fail
args (\d+) (\d+) = echo {0} {1}
args (\w+) (\d+) = echo {0} {1}
not_matching2 = fail
bad_escape (\w\=+) = echo {0}
bad_regex (\w=+) = echo {0}
bad_formatting (\d+) = echo {1}
not_found = not_found
